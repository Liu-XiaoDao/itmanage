<!DOCTYPE html>
<html lang="zh-cn">
  <head>
    <meta charset="utf-8"/>
    <meta name="viewport" content="width=device-width, initial-scale=1"/>
    <title><%= title yield(:pre_title) %></title>
    <link href="http://cdn.bootcss.com/bootstrap/3.3.7/css/bootstrap.min.css" rel="stylesheet"/>
    <%= csrf_meta_tags %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
  </head>
  <body>
    <div class="container" style="padding-top:70px;">
      <div class="row">
        <div class="col-sm-12 col-md-12 center-block" style="float: none;">
          <div class="table-responsive" style="padding-top: 20px;">
            <table class="table table-striped table-bordered">
              <thead>
                  <tr><th>#</th><th>网站名称</th><th>网址</th><th>介绍</th><th>请求者</th><th>是否添加</th></tr>
              </thead>
              <tbody>
            		<% @white_lists.each do |white_list| %>
            			<tr>
                    <td><%= white_list.id %></td>
        	    			<td><%= link_to white_list.web_name, "#{white_list.url}" %></td>
        	    			<td><%= white_list.url %></td>
                    <td><%= white_list.introduce %></td>
                    <td><%= white_list.requester %></td>
                    <td><%= white_list.is_add == 0 ? "未添加" : "已添加" %></td>
          		    </tr>
                <% end %>
              </tbody>
            </table>
          </div>
          <%= will_paginate @white_lists, renderer: WillPaginate::ActionView::BlogLinkRenderer %>
        </div>
      </div>
    </div>
  </body>
</html>
